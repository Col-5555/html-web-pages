# Port the Express API listens on. 4000 is a safe default on this shared machine
# (3000 and 8080 are held by other users; the managers app uses 8457/3457).
# Copy this file to .env and adjust if 4000 is busy.
PORT=4000
