# Base URL of the coders-app-api backend. Vite exposes any VITE_-prefixed var to
# the client as import.meta.env.VITE_API_URL. Copy this file to .env for local dev.
VITE_API_URL=http://localhost:4000
